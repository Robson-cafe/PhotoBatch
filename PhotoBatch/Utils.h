#pragma once

#include <string>

using namespace std;

//-------------------------------------------------------------------------

namespace Utils
{
	string ToLower(
		string str
	);

	bool HasWhiteSpace(
		const string& flag
	);
}

